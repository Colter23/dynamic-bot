package top.colter.dynamic.draw.theme

import top.colter.dynamic.core.data.Dynamic
import top.colter.skiko.layout.Layout

fun Layout.DefaultDynamicTheme(
    dynamic: Dynamic,
) {
    // Placeholder for future theme customization.
}
